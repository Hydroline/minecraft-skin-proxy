# Stored only on the production host. Do not commit deploy/.env.
# CNB registry pull token with read-only package permission.
CNB_DOCKER_PULL_TOKEN=replace-with-read-only-cnb-token

# The host port that self-hosted Nginx will reverse proxy to.
ADAPTER_HOST_PORT=18080
